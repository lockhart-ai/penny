# penny-client
iOS client for the penny personal assistant

Build and test from the repo root with `make client-check` (requires Xcode; runs
`PennyClientTests` on a freshly booted simulator). CI runs the same gate on any
pull request touching `penny-client/`.
