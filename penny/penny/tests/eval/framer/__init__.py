"""The skill framer's contract — the microcontext that reads a demonstrated round
and settles the routine's interface: which of its values are parameters, and which
are constants the routine is about."""
