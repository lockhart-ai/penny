"""The collector's contract — the background agent that runs a standing routine on
its own schedule: what it enacts, what it notifies, and how it behaves when a write
duplicates, a key is missing, or the world it reads is down."""
